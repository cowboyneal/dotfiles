#!/usr/bin/env bash

muted=$(amixer get Master | grep \[[0-9]*%\] | sed '1q;d' | awk '{ print $6 }')

if [ "$muted" == "[off]" ]; then
    echo 'Muted'
    exit
else
    amixer get Master | grep \[[0-9]*%\] | sed '1q;d' | awk '{ print $5 }' | \
        sed -E 's/\[|\]//g' | sed 's/%//'
fi
